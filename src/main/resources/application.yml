server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rospiel/study-configs
          username: "${GITHUB_USER}"
          password: "${GITHUB_PASS}"

